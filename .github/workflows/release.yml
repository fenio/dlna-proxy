name: Release Binaries

on:
  push:
    tags:
      - "v*.*.*"
  workflow_dispatch:

env:
  CARGO_TERM_COLOR: always

jobs:
  build-linux:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        include:
          - target: x86_64-unknown-linux-musl
            artifact_name: dlna-proxy-linux-amd64
            use_nightly: false
          - target: aarch64-unknown-linux-musl
            artifact_name: dlna-proxy-linux-arm64
            use_nightly: false
          - target: mips-unknown-linux-musl
            artifact_name: dlna-proxy-linux-mips
            use_nightly: true
          - target: mipsel-unknown-linux-musl
            artifact_name: dlna-proxy-linux-mipsel
            use_nightly: true
          - target: armv7-unknown-linux-musleabihf
            artifact_name: dlna-proxy-linux-armv7l
            use_nightly: false

    steps:
      - name: Checkout repository
        uses: actions/checkout@v6

      - name: Install Rust (stable)
        if: ${{ !matrix.use_nightly }}
        uses: dtolnay/rust-toolchain@stable

      - name: Install Rust (nightly)
        if: ${{ matrix.use_nightly }}
        uses: dtolnay/rust-toolchain@nightly
        with:
          components: rust-src

      - name: Install cross
        run: cargo install cross --git https://github.com/cross-rs/cross

      - name: Build binary
        run: cross build --release --target ${{ matrix.target }}

      - name: Rename binary
        run: |
          cp target/${{ matrix.target }}/release/dlna-proxy ${{ matrix.artifact_name }}

      - name: Upload artifact
        uses: actions/upload-artifact@v6
        with:
          name: ${{ matrix.artifact_name }}
          path: ${{ matrix.artifact_name }}

  build-windows:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        include:
          - target: x86_64-pc-windows-gnu
            artifact_name: dlna-proxy-windows-amd64.exe
          - target: i686-pc-windows-gnu
            artifact_name: dlna-proxy-windows-x86.exe

    steps:
      - name: Checkout repository
        uses: actions/checkout@v6

      - name: Install Rust
        uses: dtolnay/rust-toolchain@stable

      - name: Install cross
        run: cargo install cross --git https://github.com/cross-rs/cross

      - name: Build binary
        run: cross build --release --target ${{ matrix.target }}

      - name: Rename binary
        run: |
          cp target/${{ matrix.target }}/release/dlna-proxy.exe ${{ matrix.artifact_name }}

      - name: Upload artifact
        uses: actions/upload-artifact@v6
        with:
          name: ${{ matrix.artifact_name }}
          path: ${{ matrix.artifact_name }}

  build-macos:
    runs-on: macos-latest
    strategy:
      matrix:
        include:
          - target: x86_64-apple-darwin
            artifact_name: dlna-proxy-macos-amd64
          - target: aarch64-apple-darwin
            artifact_name: dlna-proxy-macos-arm64

    steps:
      - name: Checkout repository
        uses: actions/checkout@v6

      - name: Install Rust
        uses: dtolnay/rust-toolchain@stable
        with:
          targets: ${{ matrix.target }}

      - name: Build binary
        run: cargo build --release --target ${{ matrix.target }}

      - name: Rename binary
        run: |
          cp target/${{ matrix.target }}/release/dlna-proxy ${{ matrix.artifact_name }}

      - name: Upload artifact
        uses: actions/upload-artifact@v6
        with:
          name: ${{ matrix.artifact_name }}
          path: ${{ matrix.artifact_name }}

  release:
    needs: [build-linux, build-windows, build-macos]
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - name: Download all artifacts
        uses: actions/download-artifact@v7
        with:
          path: artifacts

      - name: Prepare release files
        run: |
          mkdir -p release
          for dir in artifacts/*/; do
            cp "$dir"* release/
          done
          cd release && ls -la

      - name: Create checksums
        run: |
          cd release
          sha256sum * > checksums-sha256.txt
          cat checksums-sha256.txt

      - name: Release
        uses: softprops/action-gh-release@v2
        if: startsWith(github.ref, 'refs/tags/')
        with:
          files: |
            release/*
          generate_release_notes: true
